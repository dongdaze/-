  this指向，一直想把这个this说一下，最近找时间又过了一遍，下面是我自己总结的经验

  this是有优先级的，我先从优先级比较低的情况开始说

  1.在普通函数中，this通常是指向window的，也就是全局

  2.在一个函数作为一个对象的属性中的值时候，也就是作为对象的一个方法的时候，这时候this就指向对象

  3.还有一种方法，常常是用来改变this指向的，就是采用call,apply,bind，它们通过绑定对象来改变this的指向，绑定谁指定谁，
  call需要把参数一个个传入，而apply可以把参数以数组的形式传入；

  bind是ES5的内容，对浏览器的兼容要求较高，需要IE8之后才能支持；而且bind方法会创建一个新函数,称为绑定函数.当绑定函数的
  一个参数作为this传入的话,传入bind方法的第二个以及以后的参数会按照顺序作为原函数的参数来调用原函数.这个情况可以用来做一
  些初始化模块，便于调用。

  4.当使用new构造一个对象的时候，这个this就指向了这个新对象
